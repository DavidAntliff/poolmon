#!/bin/bash

cd $(dirname $0)
. ../services/common

if ./check-influxdb ; then
  docker run \
      --rm \
      --tty \
      --interactive \
      --name influxdb-cli \
      --network $NETWORK \
      poolmon/influxdb \
      influx -host influxdb
fi
